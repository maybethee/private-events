<h1><%= @event.title %></h1>

<div>To be held on <%= @event.date %></div>
<div>at <%= @event.location %></div>

<%= button_to 'Join Event', event_attendances_path(@event), method: :post %>

<h2>Attendees:</h2>

<% @event.attendees.each do |attendee| %>
  <div><%= attendee.name %></div>
<% end %>